"use client";

import { Container, Text } from "@chakra-ui/react";

const Blogs = ({ params }) => {
  //   const response = fetch(`${process.env.REACT_APP_BASE_URL}`);

  return (
    <Container size="container.lg">
      <Text>Coming Soon...</Text>
    </Container>
  );
};

export default Blogs;
